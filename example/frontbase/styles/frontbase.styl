/*!
	Frontbase by Clevisaci
	- a starter fuel for web projects */

// Typography
$base-font-size ?= 16px
$font-family-sans-serif ?= 'Helvetica Neue', Helvetica, Arial, sans-serif
$font-family-serif ?= Georgia, Cambria, 'Times New Roman', Times, serif
$font-family-monospace ?= Monaco, Menlo, Consolas, 'Courier New', monospace
$font-family-base ?= $font-family-sans-serif

html
	font-size: $base-font-size
	-webkit-overflow-scrolling: touch
	-webkit-tap-highlight-color: rgba(0,0,0,0)
body
	font-family: $font-family-base
	overflow-y: scroll

// IE10 Metro responsive
// Required for Windows 8 Metro split-screen snapping with IE10
// Source: http://timkadlec.com/2012/10/ie10-snap-mode-and-responsive-design/
@-ms-viewport
	width: device-width

// IE10 on Windows Phone 8
// IE10 on WP8 doesn't report CSS pixels, but actual device pixels. In
// other words, say on a Lumia, you'll get 768px as the device width,
// meaning users will see the tablet styles and not phone styles.
// Source: http://timkadlec.com/2013/01/windows-phone-8-and-device-width/
@media screen and (max-width: 400px)
	@-ms-viewport
		width: 320px

// Responsive images (ensure images don't scale beyond their parents)
img
	max-width: 100% // Part 1: Set a maxium relative to the parent
	height: auto // Part 2: Scale the height according to the width, otherwise you get stretching
	vertical-align: middle // Match vertical alignment with other comment elements

// Tools
.accessible
	position: absolute !important
	clip: rect(1px 1px 1px 1px) /* IE6, IE7 */
	clip: rect(1px, 1px, 1px, 1px)
// Hide from screenreaders and browsers
.hidden
	display: none
	visibility: hidden

// WebKit bug documented here: https://bugs.webkit.org/show_bug.cgi?id=82489
svg
	max-height: 100%

// Convert px to ems based on base size
em($size, $base = $base-font-size)
	$size / $base em

// Convert px to rems with px fallback
rem($size)
	if current-property
		add-property(current-property, $size px)
		$size / $base-font-size rem
	else
		error('rem() must be used within a property')

// Generate break variable to use in media queries
break($width)
	'only screen and (min-width: ' + em($width) + ')'
breakH($height)
	'only screen and (min-height: ' + em($height) + ')'
breakMax($width)
	'only screen and (max-width: ' + em($width) + ')'
breakMaxH($height)
	'only screen and (max-height: ' + em($height) + ')'

// Layout breakpoints
$break0 = break(320)
$break1 = break(480)
$break2 = break(768)
$break3 = break(992)
$break4 = break(1200)

$landscape = 'only screen and (orientation: landscape)'
$portrait = 'only screen and (orientation: portrait)'
